{
  "name": "@seance/relay",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object routing opaque blobs between the app and daemons",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "bun test --timeout 15000",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test"
  },
  "dependencies": {
    "@seance/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260727.1",
    "@types/bun": "^1.3.14",
    "miniflare": "^4.20260722.0",
    "wrangler": "^4.114.0"
  }
}
