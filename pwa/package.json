{
  "name": "@seance/pwa",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Workers app — machine list, repo picker, spawn form, spawn verdict",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "deploy": "vite build && wrangler deploy",
    "dev": "vite",
    "preview": "vite preview",
    "test": "bun test --timeout 15000",
    "typecheck": "tsc --noEmit && tsc --noEmit -p test"
  },
  "dependencies": {
    "@seance/shared": "workspace:*",
    "preact": "^10.28.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/bun": "^1.3.14",
    "miniflare": "^4.20260722.0",
    "vite": "^8.2.0",
    "vite-plugin-pwa": "^1.3.0",
    "wrangler": "^4.114.0"
  }
}
