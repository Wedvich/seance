{
  "name": "@seance/daemon",
  "version": "0.0.0",
  "private": true,
  "description": "seanced — per-machine daemon that holds the relay socket and spawns claude in tmux",
  "license": "MIT",
  "bin": {
    "seanced": "./src/main.ts"
  },
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@seance/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
